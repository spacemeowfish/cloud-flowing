# nginx.service drop-in：网关依赖顺序（先起若依与 agent，nginx 最后对外）
# 安装位置：/etc/systemd/system/nginx.service.d/gateway.conf
# 用法：systemctl daemon-reload && systemctl enable --now nginx

[Unit]
After=network-online.target ruoyi.service agent-platform.service
Wants=ruoyi.service agent-platform.service
