# RK3588 真机验收表

## 版本与产物

- [ ] 系统镜像、内核、RKNPU 驱动、RKLLM Toolkit/Runtime、官方 Server commit 已记录。
- [ ] Qwen2.5-3B-Instruct source revision 是精确 commit，不是分支或 `latest`。
- [ ] `model-manifest.json` 中配置、校准集和 `.rkllm` SHA256 与板上文件一致。
- [ ] 模型文件权限仅允许服务用户读取；环境文件权限为 `0600`。

## Runtime 与协议

- [ ] 官方 `llm_demo` 能加载模型并完成一条中文生成。
- [ ] `/v1/models` 通过冻结契约，Server 只可由本机访问。
- [ ] 正常 JSON、完整 Markdown fence、前置 `<think>`、非法 JSON、400、503 和超时路径符合 Adapter 契约。
- [ ] 两个并发请求不会并行进入 NPU；第二个请求按配置排队或返回可重试繁忙错误。
- [ ] 取消请求不会触发云回退或工具执行。

## Agent 回归

- [ ] 上板后全量 Python 测试通过，记录实际数量和命令输出。
- [ ] 60 条固定评测全部执行并保存 raw JSONL，不使用 PC Mock 结果代替。
- [ ] Schema compliance 不低于 98%，意图准确率不低于 95%，端到端准确率不低于 90%。
- [ ] 删除/取消仍在工具调用前停在 `awaiting_confirmation`。
- [ ] D2/D3 和未知等级在本地繁忙/超时时不会发往云端。

## 性能与稳定性

- [ ] 冷启动、模型加载、首 Token、完整意图生成、Tokens/s、峰值 RSS/NPU 内存均已记录。
- [ ] 分别测试 192、512 Token 上限和 4096 上下文预算，确认无 OOM。
- [ ] 连续 100 次结构化意图无 Runtime 崩溃、死锁或无法恢复的繁忙状态。
- [ ] 至少 8 小时长稳；记录温度、是否降频、错误率和 warmup 后内存增长。
- [ ] 断开网络、重启 RKLLM、重启 Agent、磁盘空间不足和 Runtime 失败均有可诊断日志。

## 外设与系统集成

- [ ] Linux 文件打开实现或禁用策略已确认，未沿用 Windows 行为假设。
- [ ] 通知、音频、麦克风和其他外设只对实际存在的硬件声明支持。
- [ ] systemd 启停顺序、自动重启、日志轮转、数据目录权限和异常断电恢复已验证。

## 签收

- 板卡/内存：
- 验收日期：
- 测试报告路径：
- 失败项与处理决定：
- 签收人：
